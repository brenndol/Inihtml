import { ArrumandoWikiPipe } from "./arrumando-wiki.pipe";


describe('ArrumandoWikiPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrumandoWikiPipe();
    expect(pipe).toBeTruthy();
  });
});
