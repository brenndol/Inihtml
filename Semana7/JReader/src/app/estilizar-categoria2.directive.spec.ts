import { EstilizarCategoria2Directive } from './estilizar-categoria2.directive';

describe('EstilizarCategoria2Directive', () => {
  it('should create an instance', () => {
    const directive = new EstilizarCategoria2Directive();
    expect(directive).toBeTruthy();
  });
});
